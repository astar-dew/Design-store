# _inbox

발견 즉시 태깅할 시간이 없을 때 **스크린샷만 여기 던져둔다.**
(이 폴더의 이미지는 git에 커밋되지 않는다)

나중에 몰아서 등록:

```bash
scripts/add-ref.sh linear-issue-list --inbox     # _inbox 이미지 전부를 이 레퍼런스로
```

등록 후 `_inbox`는 직접 비운다.

> 캡처 시점에 5분씩 쓰라고 하면 안 하게 된다. 던져두고 주 1회 정리하는 게 유지된다.
