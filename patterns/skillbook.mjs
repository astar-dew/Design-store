// 디자인 스킬 — 현업에서 UI 품질을 가르는 판단 기준을, 영향도 순으로 정리한 것.
//
// 이 저장소의 다른 탭이 "무엇을 만들까"(스킨·패턴·분야)를 다룬다면
// 여기는 "잘 만들었는가"를 다룬다. 외주를 줄 때는 요구사항으로,
// 받아올 때는 검수 기준으로 그대로 쓴다.
//
// 순서는 취향이 아니라 영향도다. 1~2번이 깨지면 예쁜 화면도 못 쓰는 화면이 되고,
// 9~10번은 안 지켜도 대개 티가 안 난다. 시간이 모자라면 위에서부터 지킨다.
//
// here: 이 저장소가 그 항목을 실제로 어디서 어떻게 다루는지. 말뿐인 원칙이 되지 않게 한다.

export const LEVELS = {
  CRITICAL: { label: '치명', note: '깨지면 못 쓰는 화면이 된다' },
  HIGH: { label: '높음', note: '전문성 인상이 여기서 갈린다' },
  MEDIUM: { label: '보통', note: '완성도 차이로 읽힌다' },
  LOW: { label: '낮음', note: '해당될 때만 챙긴다' },
}

export const SKILLBOOK = [
  {
    id: 'a11y', pri: 1, level: 'CRITICAL', name: '접근성', en: 'Accessibility',
    lede: '색약·저시력·키보드 전용 사용자가 못 쓰면 그 화면은 기능이 없는 것과 같다. 공공·의료·금융 발주는 아예 계약 조건에 들어간다.',
    must: [
      ['명도 대비 4.5:1', '본문 기준. 큰 글씨·아이콘 등 비텍스트는 3:1. 다크모드는 따로 다시 잰다 — 라이트에서 통과했다고 다크가 통과하지 않는다'],
      ['포커스 링을 지우지 않는다', '2~4px 가시 링. 보기 싫다고 outline:none 하는 순간 키보드 사용자는 자기 위치를 잃는다'],
      ['아이콘 단독 버튼에 이름', 'aria-label. 텍스트가 옆에 있으면 아이콘은 aria-hidden으로 빼서 두 번 읽히지 않게 한다'],
      ['색으로만 알리지 않는다', '빨강=오류라면 아이콘이나 문구를 같이 준다. 남성 20명 중 1명이 적록을 구분하지 못한다'],
      ['탭 순서 = 시각 순서', 'DOM 순서를 바꾸는 CSS(order, grid-area)는 읽는 순서를 조용히 어긋나게 한다'],
      ['스티키 UI가 포커스를 가리지 않게', '상단 고정 헤더 밑으로 포커스된 입력이 숨는 건 WCAG 2.2 신규 위반이다 (scroll-margin-top)'],
    ],
    avoid: ['outline:none 만 넣고 대체 스타일 없음', '플레이스홀더를 라벨 대신 사용', '회색 위 옅은 회색 본문'],
    here: '말로만 지키지 않는다 — scripts/check-contrast.mjs 가 스킨 14종 × 2모드 × 7규칙을 검사하고 미달이면 빌드를 멈춘다. 도입할 때 9종이 걸렸다(34건). 다크에서 도넛의 옅은 단계가 배경에 묻히던 문제도 투명도 대신 표면색과 섞어 해결했다.',
  },
  {
    id: 'touch', pri: 2, level: 'CRITICAL', name: '터치·인터랙션', en: 'Touch & Interaction',
    lede: '반응이 없으면 사용자는 고장으로 읽고 두 번 누른다. 결제·전송에서 두 번 눌림은 사고가 된다.',
    must: [
      ['탭 영역 최소 44×44', 'iOS 44pt / Android 48dp. 아이콘이 20px라도 히트 영역은 패딩으로 늘린다. 웹 포인터는 24×24 CSS px가 하한'],
      ['타깃 사이 8px 이상', '붙어 있으면 오탭이 난다. 특히 삭제 버튼이 옆에 있을 때'],
      ['100ms 안에 눌린 티', '리플·불투명도·엘리베이션 중 하나. 즉시 반응이 없으면 지연이 아니라 무반응으로 느껴진다'],
      ['비동기 중 버튼 잠금', '스피너 + disabled. 중복 제출 방지는 서버가 아니라 여기서 먼저 한다'],
      ['호버에만 기능을 걸지 않는다', '터치 기기에는 호버가 없다. 호버는 힌트, 클릭이 기능'],
      ['눌림 효과가 레이아웃을 밀지 않게', 'transform/opacity로. width·padding을 건드리면 주변이 흔들린다'],
    ],
    avoid: ['호버해야 나타나는 유일한 액션', '0ms 즉시 전환', '눌러도 아무 변화 없는 카드'],
    here: '스킨 전용 페이지 하단의 컴포넌트 상태 데모가 이 항목의 증명이다. 호버·눌림·포커스·비활성이 실제로 동작해서 클라이언트가 직접 만져볼 수 있다 — scripts/build-skins.mjs',
  },
  {
    id: 'perf', pri: 3, level: 'HIGH', name: '성능', en: 'Performance',
    lede: '느린 화면은 못생긴 화면보다 빨리 버려진다. 특히 레이아웃이 튀는 건 디자인 실력 문제로 오해받는다.',
    must: [
      ['이미지에 width/height', 'aspect-ratio도 가능. 이걸 빼면 이미지가 로드될 때 아래 내용이 통째로 밀린다 (CLS)'],
      ['WebP/AVIF + 반응형 srcset', 'PNG 스크린샷을 그대로 올리면 용량이 5~10배가 된다'],
      ['폰트 font-display:swap', '없으면 폰트 받는 동안 글자가 아예 안 보인다 (FOIT)'],
      ['비동기 영역에 자리 예약', '스켈레톤은 장식이 아니라 자리 확보 장치다. 1초 넘는 작업에 스피너 대신 스켈레톤'],
      ['50개 넘는 리스트는 가상화', '스크롤이 끊기는 건 대개 렌더 개수 문제지 애니메이션 문제가 아니다'],
      ['스크롤·리사이즈·입력은 디바운스', '프레임당 16ms 예산을 지키는 가장 싼 방법'],
    ],
    avoid: ['화면 폭이 늘었다 줄었다 하는 로딩', '모든 폰트 variant를 preload', '접힌 영역 이미지까지 즉시 로드'],
    here: 'add-ref.sh가 등록 시점에 WebP로 변환하고 최대폭 1440px·q75로 낮춘다. 원본 PNG는 .gitignore로 커밋에서 뺀다 — scripts/add-ref.sh',
  },
  {
    id: 'style', pri: 4, level: 'HIGH', name: '스타일 선택', en: 'Style Selection',
    lede: '스타일은 취향이 아니라 제품 유형에 대한 답이다. 틀린 스타일은 잘 만들어도 어색하다.',
    must: [
      ['제품 유형에 맞춘다', '금융에 네온, 게임에 스위스 미니멀은 각각 불신과 지루함을 만든다'],
      ['한 제품에 한 스타일', '페이지마다 스타일이 다르면 같은 서비스로 안 읽힌다'],
      ['이모지를 아이콘으로 쓰지 않는다', 'OS마다 모양이 다르고 색을 못 바꾼다. SVG 아이콘셋 하나로 통일'],
      ['그림자 스케일을 정해둔다', '카드·시트·모달에 각각 정해진 단계. 매번 다른 값은 정리 안 된 인상을 준다'],
      ['라이트/다크를 같이 설계', '나중에 뒤집는 게 아니라 처음부터 쌍으로. 이게 다크모드 추가금액의 근거다'],
      ['주 액션은 화면당 하나', '강조가 셋이면 강조가 없는 것과 같다'],
    ],
    avoid: ['플랫과 스큐어모피즘 혼용', '아이콘셋 두세 개 섞어 쓰기', '값이 제각각인 그림자'],
    here: '스킨 10종에 각각 lineage(계보)·bench(실제 제품 3개)·fits(맞는 분야)·watch(약점)를 붙였다. "감성"이 아니라 근거로 고르게 하기 위한 것 — docs/01-design-taxonomy.md',
  },
  {
    id: 'layout', pri: 5, level: 'HIGH', name: '레이아웃·반응형', en: 'Layout & Responsive',
    lede: '모바일에서 가로 스크롤이 생기는 순간, 그 화면은 검수에서 떨어진다.',
    must: [
      ['모바일 우선으로 짠다', '좁은 데서 넓히는 건 쉽고, 넓은 데서 줄이는 건 매번 깨진다'],
      ['브레이크포인트를 고정', '375 / 768 / 1024 / 1440. 컴포넌트마다 다른 숫자를 쓰면 관리가 안 된다'],
      ['본문 16px 하한', '모바일에서 16px 미만 입력은 iOS가 자동 확대해버린다'],
      ['한 줄 글자수 제한', '모바일 35~60자, 데스크톱 60~75자. 태블릿에서 문단이 화면 끝까지 가면 못 읽는다'],
      ['간격은 4/8 배수', '임의 값(13px, 27px)이 섞이면 리듬이 무너진다'],
      ['고정 바만큼 여백 예약', '스티키 헤더·하단 CTA 아래에 콘텐츠가 숨지 않게'],
      ['100vh 대신 100dvh', '모바일 브라우저 주소창 때문에 100vh는 실제 높이가 아니다'],
    ],
    avoid: ['가로 스크롤', 'user-scalable=no', '컨테이너 폭을 px로 고정'],
    here: '스킨 프리뷰는 컨테이너 쿼리로 스케일한다. --u:calc(100cqw/480) 같은 길이 단위를 곱해서 쓴다 — transform:scale(calc(100cqw/480))은 길이라서 scale()에 안 들어간다',
  },
  {
    id: 'type', pri: 6, level: 'MEDIUM', name: '타이포·컬러', en: 'Typography & Color',
    lede: '가독성은 폰트 선택이 아니라 행간·자간·대비에서 결정된다. 폰트만 바꾸고 나머지를 안 만지면 아무것도 안 바뀐다.',
    must: [
      ['본문 행간 1.5~1.75', '가장 싸게 완성도를 올리는 값 하나를 고르라면 이것'],
      ['타입 스케일 고정', '12 14 16 18 24 32처럼. 눈대중으로 15px, 17px를 섞으면 위계가 안 생긴다'],
      ['굵기로 위계', '제목 600~700 / 본문 400 / 라벨 500. 크기만으로 위계를 만들면 화면이 커진다'],
      ['색은 시맨틱 토큰으로', 'primary·surface·on-surface. 컴포넌트에 #3b82f6을 직접 박으면 테마 교체가 불가능해진다'],
      ['다크는 반전이 아니다', '채도를 낮추고 명도를 올린 톤 변형. 순검정·순백은 쓰지 않는다'],
      ['숫자는 tabular-nums', '금액·타이머·표에서 자릿수가 흔들리지 않게'],
      ['자르기보다 줄바꿈', '어쩔 수 없이 자를 때만 말줄임 + 전체 텍스트를 볼 방법을 준다'],
    ],
    avoid: ['본문 12px 미만', '회색 배경 위 회색 글자', '컴포넌트에 raw hex'],
    here: '스킨마다 --s-bg/sur/line/fg/dim/acc/font/headWeight/tracking 토큰 세트를 갖는다. --s-r(라운드)와 --s-bw(선 굵기)는 단위 없는 숫자라 스케일 단위와 곱할 수 있다 — patterns/skins.mjs',
  },
  {
    id: 'motion', pri: 7, level: 'MEDIUM', name: '애니메이션', en: 'Animation',
    lede: '움직임은 장식이 아니라 인과 설명이다. 왜 움직이는지 말할 수 없으면 빼는 게 낫다.',
    must: [
      ['transform/opacity만 애니메이션', 'width·height·top·left는 매 프레임 레이아웃을 다시 계산한다'],
      ['거리·복잡도에 맞춰 시간 결정', '전 구간에 200ms 하나를 복사해 쓰는 게 가장 흔한 실수다'],
      ['나갈 때는 들어올 때의 60~70%', '퇴장이 느리면 앱 전체가 굼떠 보인다'],
      ['이징에 의미를 준다', '도착은 감속, 퇴장은 가속, 등속은 진짜 일정 속도(로딩 회전)에만'],
      ['화면당 움직이는 요소 1~2개', '동시에 다 움직이면 어디를 봐야 할지 알 수 없다'],
      ['prefers-reduced-motion 존중', '전정기관 장애가 있으면 패럴랙스가 실제로 어지럼증을 유발한다'],
      ['중간에 끊을 수 있게', '사용자 입력이 진행 중 애니메이션을 즉시 취소해야 한다. 애니메이션 종료 이벤트에 로직을 의존하지 않는다'],
    ],
    avoid: ['모든 전환에 같은 duration', '입력을 막는 애니메이션', 'reduced-motion 미대응 패럴랙스'],
    here: '이 저장소 UI 자체가 대상이다. 카드 호버는 transform과 box-shadow만 쓰고, prefers-reduced-motion에서는 전부 끈다 — scripts/build-index.mjs',
  },
  {
    id: 'form', pri: 8, level: 'MEDIUM', name: '폼·피드백', en: 'Forms & Feedback',
    lede: '폼은 사용자가 가장 많이 실패하는 곳이다. 실패를 막는 게 아니라 실패에서 빠져나오게 하는 게 설계다.',
    must: [
      ['라벨은 항상 보이게', '플레이스홀더는 입력을 시작하면 사라진다 — 그 순간 무슨 칸인지 알 수 없다'],
      ['오류는 해당 칸 바로 아래', 'aria-describedby로 연결. 상단 요약만 있으면 어디가 틀렸는지 찾아야 한다'],
      ['오류 문구에 해결책', '"잘못된 입력"이 아니라 "숫자 11자리로 입력하세요"'],
      ['blur에 검증, 키 입력마다는 안 한다', '타이핑 중 빨간 줄은 아직 안 틀린 사람을 혼내는 것이다'],
      ['파괴적 동작은 확인 또는 되돌리기', '확인 다이얼로그보다 "실행 후 실행취소 토스트"가 대개 낫다'],
      ['비활성 상태를 분명히', '불투명도 0.38~0.5 + 커서 변경 + 실제 disabled 속성. 읽기전용과는 다르게 보여야 한다'],
      ['빈 상태에 다음 행동', '"데이터 없음"이 아니라 무엇을 하면 채워지는지'],
    ],
    avoid: ['플레이스홀더만 있는 입력', '제출 후에야 알려주는 오류', '되돌릴 수 없는 삭제'],
    here: '레퍼런스 탭의 빈 상태가 예시다. "없습니다"에서 끝내지 않고 등록 명령어를 그대로 복사할 수 있게 보여준다 — scripts/build-index.mjs',
  },
  {
    id: 'nav', pri: 9, level: 'HIGH', name: '네비게이션', en: 'Navigation',
    lede: '길을 잃은 사용자는 되돌아가지 않고 그냥 나간다. 현재 위치와 뒤로 가는 길, 두 가지만 항상 분명하면 된다.',
    must: [
      ['현재 위치를 표시', '색·굵기·인디케이터. 탭이 다섯 개인데 어디 있는지 모르는 화면이 흔하다'],
      ['뒤로 가기가 예측 가능하게', '스크롤 위치·필터·입력값을 복원한다. 목록으로 돌아가서 다시 스크롤하게 만들지 않는다'],
      ['하단 탭은 5개까지', '아이콘만 두지 말고 라벨을 같이. 아이콘만으로는 뜻이 안 통한다'],
      ['주요 화면은 URL로 도달 가능', '공유·알림·딥링크가 전부 여기 걸린다'],
      ['모달을 이동 수단으로 쓰지 않는다', '모달 안에서 다음 화면으로 계속 들어가면 사용자는 경로를 잃는다'],
      ['위험한 항목은 떨어뜨려 놓는다', '로그아웃·계정 삭제를 일반 메뉴 사이에 두지 않는다'],
      ['위치는 모든 페이지에서 같게', '페이지 유형에 따라 네비 위치가 바뀌면 매번 다시 찾아야 한다'],
    ],
    avoid: ['탭+사이드바+하단바 동시 사용', '조용히 홈으로 튀는 뒤로가기', '아이콘만 있는 네비'],
    here: '탭 상태를 URL 해시에 반영한다. 뒤로가기와 새로고침이 같은 탭을 유지하고, 링크로 특정 탭을 바로 열 수 있다 — index.html#skill',
  },
  {
    id: 'chart', pri: 10, level: 'LOW', name: '차트·데이터', en: 'Charts & Data',
    lede: '차트는 예쁘게 그리는 게 아니라 한 문장으로 읽히게 만드는 것이다. 읽는 데 설명이 필요하면 실패다.',
    must: [
      ['데이터 성격에 맞는 종류', '추세는 선, 비교는 막대, 비율은 도넛. 비율에 선을 쓰면 없는 연속성이 생긴다'],
      ['도넛은 5개 항목까지', '그 이상은 조각이 안 읽힌다 — 가로 막대로 바꾼다'],
      ['색에만 의미를 싣지 않는다', '패턴·직접 라벨을 같이. 적록 조합만으로 손익을 구분하지 않는다'],
      ['범례는 차트 옆에', '스크롤을 내려야 보이는 범례는 없는 것과 같다'],
      ['데이터 대비 3:1, 라벨 4.5:1', '그리드선은 반대로 흐리게 — 데이터와 경쟁하면 안 된다'],
      ['빈/오류 상태를 따로 만든다', '빈 축만 남은 차트는 고장으로 읽힌다'],
      ['텍스트 대안을 준다', '표 또는 요약 문구. 차트만으로는 스크린리더에서 아무것도 아니다'],
    ],
    avoid: ['항목 8개짜리 파이차트', '진한 그리드선', '데이터 없을 때 빈 축 프레임'],
    here: '풀 목업의 4종 차트(영역·막대·도넛·스파크라인)에서 손실 배지가 액센트색과 같아 상승으로 읽히던 스킨 3종에 별도 경고색을 넣었다 — patterns/layouts.mjs',
  },
]

// 납품 검수 체크리스트 — 외주 결과물을 받았을 때 이 순서로 본다.
// docs/03-outsourcing-brief.md 의 검수 항목과 같은 기준.
export const CHECKLIST = [
  ['시각 품질', [
    '이모지를 아이콘으로 쓴 곳이 없다',
    '아이콘이 한 세트에서 왔고 선 굵기가 같다',
    '그림자 값이 정해진 단계 안에서만 쓰였다',
    '색이 시맨틱 토큰으로 들어갔다 (컴포넌트에 raw hex 없음)',
  ]],
  ['인터랙션', [
    '모든 클릭 요소에 호버·눌림 상태가 있다',
    '탭 영역이 44×44 이상이다',
    '비활성 상태가 눈에 띄게 다르고 실제로 안 눌린다',
    '비동기 동작 중 버튼이 잠기고 진행 표시가 나온다',
  ]],
  ['라이트·다크', [
    '본문 대비가 라이트·다크 각각 4.5:1 이상이다',
    '경계선과 구분선이 두 모드 모두에서 보인다',
    '호버·포커스·비활성이 두 모드 모두 정의됐다',
    '모달 스크림이 실제 배경 위에서 측정됐다',
  ]],
  ['레이아웃', [
    '375px에서 가로 스크롤이 없다',
    '스티키 바 뒤로 콘텐츠가 숨지 않는다',
    '간격이 4/8 배수 안에서만 쓰였다',
    '태블릿에서 본문 한 줄이 너무 길지 않다',
  ]],
  ['접근성', [
    '키보드만으로 전체 흐름이 가능하다',
    '포커스 링이 모든 요소에서 보인다',
    '아이콘 단독 버튼에 이름이 있다',
    '색 없이도 상태가 구분된다',
    'prefers-reduced-motion에서 레이아웃이 깨지지 않는다',
  ]],
]

export const SKILL_COUNT = SKILLBOOK.reduce((n, c) => n + c.must.length, 0)
export const CHECK_COUNT = CHECKLIST.reduce((n, [, items]) => n + items.length, 0)
